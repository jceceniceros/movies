import styles from './styles.module.css';

const MoviesList: React.FC = (props) => {
  return (
    <div className={styles.MoviesList}>
      <h2>Movie List:</h2>
    </div>
  );
};

export {
  MoviesList
}
